<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Location capability: foreground reads, background publishing through
         a location-type foreground service, and reboot recovery. All are
         optional-feature permissions; denying them must never break the rest
         of Hearthlane. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hearthlane"
        android:usesCleartextTraffic="true">
        <!--
          Cleartext HTTP is allowed because the Hearthlane endpoints (derived
          from the base domain) are served over private transport only: the
          home LAN or the Tailscale path. This matches the pre-existing project
          behavior and does NOT disable TLS validation for HTTPS connections.
          A domain-scoped network security config cannot be used because the
          base domain is user-configured and therefore not known at build time.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Location publishing foreground service (type location). Started
             only after LocationFgsGate passes; the sharing opt-in is enforced
             by the service itself after startForeground. -->
        <service
            android:name=".location.LocationForegroundService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Reboot recovery for location publishing. Skipped unless the start
             gate passes; force-stop prevents any restart by the OS. -->
        <receiver
            android:name=".location.LocationBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
